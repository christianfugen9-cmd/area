# Example environment configuration for Frontend
# Replace <BACKEND_IP> with the IPv4 address of the laptop hosting the Laravel backend (found via `ipconfig`)
VITE_API_BASE_URL=http://<BACKEND_IP>:8000/api
